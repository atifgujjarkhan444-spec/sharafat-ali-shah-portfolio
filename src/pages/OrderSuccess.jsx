import { CheckCircle2, MessageCircle } from 'lucide-react';
import { Link } from 'react-router-dom';
export default function OrderSuccess(){const order=JSON.parse(localStorage.getItem('hss-last-order')||'null');const wa=localStorage.getItem('hss-last-whatsapp');return <section className="success-page"><div className="container"><div className="success-card"><CheckCircle2 size={64}/><span className="eyebrow">Order received</span><h1>Thank you for your order!</h1><p>Your order has been recorded on this device. For a real production store, connect the checkout to a backend/order database and payment verification.</p>{order&&<div className="order-box"><div><span>Order ID</span><strong>{order.id}</strong></div><div><span>Customer</span><strong>{order.firstName} {order.lastName}</strong></div><div><span>Total</span><strong>₨{order.total.toLocaleString('en-PK')}</strong></div></div>}<div className="success-actions">{wa&&<a className="btn btn-whatsapp" href={wa} target="_blank" rel="noreferrer"><MessageCircle size={18}/> Send Order on WhatsApp</a>}<Link className="btn btn-dark" to="/shop">Continue Shopping</Link></div></div></div></section>}
