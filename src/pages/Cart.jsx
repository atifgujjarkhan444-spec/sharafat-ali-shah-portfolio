import { Minus, Plus, Trash2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import PageHero from '../components/PageHero';
import { useCart } from '../context/CartContext';
const money=v=>{const currency=localStorage.getItem('hss-currency-v1')||'PKR';const locale={PKR:'en-PK',SAR:'en-SA',USD:'en-US',AED:'en-AE',KWD:'en-KW'}[currency];return new Intl.NumberFormat(locale,{style:'currency',currency,maximumFractionDigits:0}).format(v)};
export default function Cart(){const {items,subtotal,updateQuantity,removeItem,clearCart}=useCart();return <><PageHero title="Cart" crumb="Shopping Cart"/><section className="container cart-section">{!items.length?<div className="empty-state"><h2>Your cart is empty</h2><p>Add a product to start your order.</p><Link to="/shop" className="btn btn-dark">Continue Shopping</Link></div>:<div className="cart-layout"><div className="cart-table">{items.map(i=><div className="cart-row" key={i.id}><img src={i.image} alt={i.name}/><div><Link to={`/product/${i.slug}`}>{i.name}</Link><small>{i.category}</small></div><strong>{money(i.price)}</strong><div className="qty"><button onClick={()=>updateQuantity(i.id,i.quantity-1)}><Minus size={14}/></button><b>{i.quantity}</b><button onClick={()=>updateQuantity(i.id,i.quantity+1)}><Plus size={14}/></button></div><button className="remove-text" onClick={()=>removeItem(i.id)}><Trash2 size={16}/> Remove</button></div>)}<button className="clear-link" onClick={clearCart}>Clear cart</button></div><aside className="summary-card"><h2>Cart totals</h2><div><span>Subtotal</span><strong>{money(subtotal)}</strong></div><div><span>Shipping</span><span>Calculated at checkout</span></div><div className="summary-grand"><span>Total</span><strong>{money(subtotal)}</strong></div><Link className="btn btn-dark full" to="/checkout-2">Proceed to Checkout</Link></aside></div>}</section></>}
