import { Eye, EyeOff } from 'lucide-react';
import { useState } from 'react';
import PageHero from '../components/PageHero';
export default function Account(){const [show,setShow]=useState(false);const [login,setLogin]=useState({user:'',password:'',remember:false});const [email,setEmail]=useState('');const [message,setMessage]=useState('');const submit=e=>{e.preventDefault();setMessage('Demo form submitted. Connect your authentication API for real accounts.');};return <><PageHero title="My Account" crumb="My Account"/><section className="container account"><div className="account-col"><h2>Login</h2><form onSubmit={submit}><label>Username or email address *<input required value={login.user} onChange={e=>setLogin({...login,user:e.target.value})}/></label><label>Password *<span className="password-wrap"><input required type={show?'text':'password'} value={login.password} onChange={e=>setLogin({...login,password:e.target.value})}/><button type="button" onClick={()=>setShow(v=>!v)}>{show?<EyeOff/>:<Eye/>}</button></span></label><div className="login-row"><button className="btn btn-dark">Log in</button><label className="check"><input type="checkbox" checked={login.remember} onChange={e=>setLogin({...login,remember:e.target.checked})}/> Remember me</label></div><a href="#forgot" onClick={e=>{e.preventDefault();setMessage('Password reset requires a real email service/backend.')}}>Lost your password?</a></form></div><div className="account-col"><h2>Register</h2><form onSubmit={submit}><label>Email address *<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><p>A link to set a new password will be sent to your email address.</p><small>Your personal data will be used to support your experience throughout this website, to manage access to your account, and for other purposes described in our privacy policy.</small><button className="btn btn-dark">Register</button></form></div></section>{message&&<div className="toast">{message}</div>}</>}
